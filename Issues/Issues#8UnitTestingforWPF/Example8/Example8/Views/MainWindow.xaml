<Window x:Class="Example8.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Example8"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Margin="5" Text="{Binding Numero1, UpdateSourceTrigger=PropertyChanged}" />
        <TextBox Grid.Row="1" Margin="5" Text="{Binding Numero2, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Row="2" Margin="5" Content="Sumar" Command="{Binding SumarCommand}" />
        <TextBlock Grid.Row="3" Margin="5" Text="{Binding Resultado}" FontWeight="Bold" FontSize="16" />
    </Grid>
</Window>
